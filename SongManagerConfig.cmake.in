# SongManagerConfig.cmake.in

@PACKAGE_INIT@

# 查找我们自己的依赖项 (spdlog 和 jsoncons)
# 因为我们的库是私有链接它们，所以使用者本来不知道这些依赖
# 我们必须在这里帮使用者找到它们
find_dependency(spdlog REQUIRED)
find_dependency(jsoncons REQUIRED)

# 包含由 install(EXPORT) 生成的 Targets 文件
include("${CMAKE_CURRENT_LIST_DIR}/SongManagerTargets.cmake")